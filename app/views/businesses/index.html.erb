<main role="main" class="col-sm-9 ml-sm-auto col-md-10 pt-3">
  <h2>Businesses</h1>
  <div class="table-responsive">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Company Name</th>
          <th>Email</th>
          <th>Primary Contact</th>
          <th>Last Contact Date</th>
          <th>Last Order Fulfillment Date</th>
        </tr>
      </thead>
      <tbody>
        <% @businesses.each do |business| %>
          <tr>
            <td><%= link_to business.company_name, business %></td>
            <td><%= business.email %></td>
            <td><%= business.primary_contact_name %></td>
            <td><%= business.last_contacted_at.strftime("%m/%d/%Y") %></td>
            <td><%= business.last_ordered_on.strftime("%m/%d/%Y") %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'New Business', new_business_path %>
  </div>
</main>
